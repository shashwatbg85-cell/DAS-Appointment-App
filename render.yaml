services:
  - type: web
    name: das-appointment-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 8080
      - key: Node_ENV
        value: production
      - key: MONGO_LOCAL_URI
        sync: false
      - key: JWT_SECRET
        sync: false
